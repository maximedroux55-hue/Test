# One article address per line. Each is fetched and read like any other story,
# but skips the relevance scoring and the date window: it is here on purpose.
#
# Lines starting with # are ignored, so notes are fine.

# Sent by Max: the Startupticker write-up naming GR3N's Series B, which the
# feeds gave us only as an amount from another outlet.
https://www.startupticker.ch/en/news/gr3n-closes-a-15-5m-series-b-round
